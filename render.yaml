# This file was generated by Render's heroku-import Heroku CLI plugin
# https://www.npmjs.com/package/@renderinc/heroku-import
# Schema documented at https://render.com/docs/yaml-spec
services:
  - type: web # valid values: https://render.com/docs/yaml-spec#type
    name: recipease
    env: node # valid values: https://render.com/docs/yaml-spec#environment
    buildCommand: npm install && yarn workspace recipease-client build && cd server && yarn run migrate:latest && ..
    startCommand: yarn workspace recipease-server start
    plan: free # optional; defaults to starter
    numInstances: 1
    envVars:
      - key: PGSSLMODE # Imported from Heroku app
        value: no-verify
      - key: DATABASE_URL # Render PostgreSQL database
        fromDatabase:
          name: postgresql-defined-69291
          property: connectionString

databases: # Render PostgreSQL database
  - name: postgresql-defined-69291
    ipAllowList: [] # only allow internal connections
